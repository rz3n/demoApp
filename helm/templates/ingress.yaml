# helm/templates/ingress.yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: demoApp
  namespace: demoApp
spec:
  ingressClassName: cloudflare-tunnel
  rules:
  - host: {{ .Values.demoApp.externalUrl }}
    http:
      paths:
      - backend:
          service:
            name: {{ .Release.Name }}-demoApp
            port:
              number: {{ .Values.demoApp.port }}
        path: /
        pathType: Prefix

